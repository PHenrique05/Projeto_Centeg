Imports System

Module Program
    Sub Main(args As String())
        Dim nomeCompleto(2) As String
        nomeCompleto(0) = "Paulo"
        nomeCompleto(1) = "Henrique"
        Console.WriteLine(nomeCompleto(0))
        Console.WriteLine(nomeCompleto(1))
    End Sub
End Module
